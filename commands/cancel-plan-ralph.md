---
description: "Cancel an active Plan Ralph Loop"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/cancel-plan-ralph.sh:*)"]
hide-from-slash-command-tool: "true"
---

# Cancel Plan Ralph

Run this command using the Bash tool to cancel the planning loop:

```
"${CLAUDE_PLUGIN_ROOT}/scripts/cancel-plan-ralph.sh"
```

Report the result to the user.
